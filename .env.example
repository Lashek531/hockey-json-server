# API key для всех /api/... эндпоинтов.
# Если оставить пустым, контейнер hockey-api сгенерирует случайный ключ при первом старте
# и выведет его в лог.
UPLOAD_API_KEY=

# Хостнейм для Traefik / Let's Encrypt (обязателен для реального HTTPS).
# Например: hockey.example.com
TRAEFIK_HOST=hockey.example.com

# E-mail для регистрации сертификатов Let's Encrypt
TRAEFIK_ACME_EMAIL=admin@example.com

# Режим импорта базы при первом старте:
#   none  - не импортировать (создать пустую базу)
#   local - импортировать из локального файла внутри контейнера
#   url   - импортировать по HTTP/HTTPS URL
DB_IMPORT_MODE=none

# Источник базы:
#   при DB_IMPORT_MODE=local: путь внутри контейнера, например: /hockey-db.zip
#   при DB_IMPORT_MODE=url:   полный URL, например: https://example.com/hockey-db.zip
DB_IMPORT_SOURCE=

# Флаг принудительного пересброса базы даже если уже инициализирована
DB_FORCE_RESET=false
